#![allow(unused)]

// Iterators
fn main() {
    let vals: Vec<u32> = vec![1, 2, 3, 4, 5];
    // into_iter ---- iterate T
    // iter      ---- iterate &T  -- can call again
    // iter_mut  ---- iterate &mut T -- can call again

    for v in vals.iter() {
        //
    }

    for v in vals.iter() {
        // 
    }
}